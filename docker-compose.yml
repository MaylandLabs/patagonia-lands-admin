version: "3.8"
services:
  patagonia-lands-admin:
    container_name: patagonia-lands-admin
    build:
      dockerfile: Dockerfile
      context: .
    restart: always
    env_file:
      - .env
    ports:
      - "5001:5000"
